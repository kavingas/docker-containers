# Virtual Hosts

<VirtualHost *:443>

    # General setup for the virtual host
    DocumentRoot "/var/www/html"
    ServerName php81.125.168.126.179.nip.io:443
    ServerAdmin you@example.com
    ErrorLog /proc/self/fd/2
    TransferLog /proc/self/fd/1

    <FilesMatch \.php$>
        SetHandler "proxy:fcgi://fpm81:9000"
        <If "%{HTTP_COOKIE} =~ /XDEBUG_SESSION=PHPSTORM/">
            SetHandler "proxy:fcgi://fpm81-debug:9000"
        </If>
    </FilesMatch>

    SSLEngine on

    SSLCertificateFile "/usr/local/apache2/conf/server.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/server.key"

    <FilesMatch "\.(cgi|shtml|phtml|php)$">
        SSLOptions +StdEnvVars
    </FilesMatch>

    <Directory "/usr/local/apache2/cgi-bin">
        SSLOptions +StdEnvVars
    </Directory>

    BrowserMatch "MSIE [2-5]" \
                 nokeepalive ssl-unclean-shutdown \
                 downgrade-1.0 force-response-1.0

    CustomLog /proc/self/fd/1 \
              "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"

</VirtualHost>


<VirtualHost *:443>

    # General setup for the virtual host
    DocumentRoot "/var/www/html"
    ServerName php82.125.168.126.179.nip.io:442
    ServerAdmin you@example.com
    ErrorLog /proc/self/fd/2
    TransferLog /proc/self/fd/1

    <FilesMatch \.php$>
        SetHandler "proxy:fcgi://fpm82:9000"
        <If "%{HTTP_COOKIE} =~ /XDEBUG_SESSION=PHPSTORM/">
            SetHandler "proxy:fcgi://fpm82-debug:9000"
        </If>
    </FilesMatch>

    SSLEngine on

    SSLCertificateFile "/usr/local/apache2/conf/server.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/server.key"

    <FilesMatch "\.(cgi|shtml|phtml|php)$">
        SSLOptions +StdEnvVars
    </FilesMatch>

    <Directory "/usr/local/apache2/cgi-bin">
        SSLOptions +StdEnvVars
    </Directory>

    BrowserMatch "MSIE [2-5]" \
                 nokeepalive ssl-unclean-shutdown \
                 downgrade-1.0 force-response-1.0

    CustomLog /proc/self/fd/1 \
              "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"

</VirtualHost>


<VirtualHost *:443>

    # General setup for the virtual host
    DocumentRoot "/var/www/html"
    ServerName php83.125.168.126.179.nip.io:443
    ServerAdmin you@example.com
    ErrorLog /proc/self/fd/2
    TransferLog /proc/self/fd/1

    <FilesMatch \.php$>
        SetHandler "proxy:fcgi://fpm83:9000"
        <If "%{HTTP_COOKIE} =~ /XDEBUG_SESSION=PHPSTORM/">
            SetHandler "proxy:fcgi://fpm83-debug:9000"
        </If>
    </FilesMatch>

    SSLEngine on

    SSLCertificateFile "/usr/local/apache2/conf/server.crt"
    SSLCertificateKeyFile "/usr/local/apache2/conf/server.key"

    <FilesMatch "\.(cgi|shtml|phtml|php)$">
        SSLOptions +StdEnvVars
    </FilesMatch>

    <Directory "/usr/local/apache2/cgi-bin">
        SSLOptions +StdEnvVars
    </Directory>

    BrowserMatch "MSIE [2-5]" \
                 nokeepalive ssl-unclean-shutdown \
                 downgrade-1.0 force-response-1.0

    CustomLog /proc/self/fd/1 \
              "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"

</VirtualHost>
